/**
 * Web Component Plugins
 *
 * This directory contains plugins that extend web component functionality.
 * Plugins can be registered globally.
 *
 * These plugins are split up to make sure we have a separation of concerns
 */

export { skeletonPlugin } from './skeleton';
export { silentErrorPlugin } from './error-boundary';
export { performancePlugin } from './performance';
