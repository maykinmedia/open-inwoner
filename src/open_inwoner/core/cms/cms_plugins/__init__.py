# This package contains CMS plugin apps
# Each plugin is a separate Django app and should be added to INSTALLED_APPS individually
# DO NOT import plugins here - it causes circular import issues during Django setup
